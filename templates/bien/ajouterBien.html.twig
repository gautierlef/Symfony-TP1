{% extends 'base.html.twig' %}

{% block title %}Ajouter bien{% endblock %}

{% block body %}
    <center><div style="width: 50%; border: 1px solid black; background-color: background"><br/>
    <h3><center>Formulaire d'ajout de bien :</center></h3>
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <br/>
        <div class="row">
            <div>
                <button type="reset" class="btn btn-danger">Annuler</button>
                <button style="margin-left: 20px" type="submit" class="btn btn-success">Valider</button>
            </div>
         </div>
    <br/>
    {{ form_end(form) }}
    </div></center>
    {% if success == 1 %}
        <br/><br/>
        <center><div class="btn btn-success">
            <a style="color:white">Bien ajouté avec succès!</a>
        </div></center>
        <br/><br/>
    {% endif %}
{% endblock %}
